{% extends 'home.html.twig' %}

{% block title %}Semestre{% endblock %}

{% block body %}
    <h1>Semestre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ semestre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ semestre.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_semestre_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_semestre_edit', {'id': semestre.id}) }}" class="btn btn-warning">edit</a>

    {{ include('semestre/_delete_form.html.twig') }}
{% endblock %}
